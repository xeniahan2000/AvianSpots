# 本地记录导入说明

从首页的“数据来源与本地导入”进入。文件仅在浏览器中读取并保存，没有上传接口，也不向观鸟记录中心请求数据。

## 文件准备

使用网页里的空白模板。CSV 需保存为 UTF-8，支持引号、引号内逗号和换行。JSON 支持记录数组，或 `{"schemaVersion":1,"records":[]}`。

每行表示一份报告中的一个分类项，本版适合定点记录。一份报告横跨多个地点或多个时段时，请先拆成独立、稳定的记录编号，保留原报告链接。否则相同来源、报告、分类版本与分类项会视为同一项，采用文件中最后一条并在预览时提示。

单次最多 5 MB / 10,000 条，本机最多 20,000 条。可以分批导入。

## 字段

| 字段 | 填写要求 |
|---|---|
| source | birdreport（观鸟记录中心）或 personal（个人）。留空使用导入界面选定的来源 |
| recordId | 必填。原报告编号或自定稳定编号；不同报告不要共用 |
| ebirdLocId | 可选。明确关联的 eBird 鸟点 ID，例如 L22179490。不会自动推断 |
| locationId | 可选。原平台地点编号 |
| locationName | 原地点名。没有 ebirdLocId 时必填 |
| observedAt | 必填。YYYY-MM-DD 或 YYYY-MM-DD HH:mm，默认北京时间。带时区的 ISO 时间会转为北京时间 |
| speciesName | 原鸟名。与 scientificName / speciesCode 至少填一项 |
| scientificName | 可选。原学名，建议保留 |
| speciesCode | 可选。已经明确核对的 eBird 分类代码。其他平台编号请填 sourceTaxonId |
| sourceTaxonId | 可选。原平台鸟种编号 |
| taxonomyVersion | 可选。原平台分类版本，建议保留 |
| count | 非负整数或 X。X 和留空均表示数量未知，不擅自当作 1 |
| url | 可选。原报告完整 http/https 链接，不能包含账号密码或脚本 |
| latitude / longitude | 同时填或同时留空。保留原坐标，不自动绘制或归并地点 |
| coordinateSystem | WGS84、GCJ02、BD09 或 unknown，默认 unknown |

常用中文表头也支持：来源、报告编号、eBird鸟点ID、原地点编号、地点名称、观察日期、鸟种名称、学名、鸟种代码、原鸟种编号、分类版本、数量、原始链接、纬度、经度、坐标系。

本功能不承诺直接兼容平台所有原始导出格式，列名不同需整理为模板。私密或敏感标记（isPrivate / isSensitive 为真）的记录会被拒绝；请先核对使用权限并脱敏。

## 预览与重复项

选择文件后先显示有效、无效、新增、更新和未改变数量。勾选本机使用许可确认后才保存。无效行不会保存，可以下载校验报告后修正。

相同来源 + 报告编号 + 分类版本 + 分类项，用相同数据再次导入不会增加条数，更新数量等字段会修改原项。不同来源即使看起来相同也会分别保存，不相加个体数。疑似跨来源重复只作提示，由用户核对。

中文名相同不自动证明是同一种。提供唯一匹配学名时，可关联到本地 eBird 分类，但界面会提示分类口径仍需核对。原名和原分类版本保留。

## 地点关联与显示范围

可在文件里填写 ebirdLocId，也可在已保存记录中展开“调整地点关联”，按名称搜索或填写 eBird ID，再确认关联整份报告。原平台地点名和坐标不会被修改。

关联后的近 30 天记录会出现在该鸟点页面，并汇总到已确认的父热点页面。没有关联、关联地点未在快照中或超出近 30 天的记录，仍能在“数据来源”页查看。当前历史名录、全局找鸟索引和地图点位不混入本地数据。

## 备份与安全

“导出本地备份”生成 JSON，之后可在同一或其他设备导入。本机数据按浏览器、网站来源和应用路径隔离，换域名、端口或部署路径不会自动迁移。

缓存修复页面 reset.html 只修复应用缓存，会保留本机导入和收藏。浏览器自带的“清除全部网站数据”则可能删除本机记录，请先备份。

导入前应确认数据属于自己或已获使用许可。只在本机查看不等于取得公开发布权。不要将备份放进公开 GitHub 仓库，也不要为了补齐数据而导入平台隐藏的敏感地点。
