# v0.3.0 测试范围

- Node.js 离线回归：30 项通过，覆盖父子热点、保留未确认地点、来源分离、CSV/JSON 校验、重复导入、备份还原、日期/数量/链接校验和 Worker 查询范围。
- Python 代理回归：3 项通过，验证父热点返回子热点时不重试地区查询、不删除原记录，空结果和未确认地点原样保留。
- 全部 JavaScript 语法检查和 Python 编译检查通过。
- 手机尺寸预览使用 390×844 像素与合成数据，不能视为真实 eBird 查询结果。
- 完整浏览器自动化脚本在缓存修复阶段之后遇到测试环境超时，未计为通过的端到端验收。缓存修复逻辑只处理应用缓存与对应 Service Worker，不清除 IndexedDB 或收藏。

本次未使用用户的 API Key 调用真实 eBird，也未运行观鸟记录中心抓取。实际 eBird 返回需在用户本地启动后核对。其他浏览器、真机和 GitHub/Worker 线上部署未做全覆盖测试。
